import { Animation } from "babylonjs"

export enum ANIM_TYPE {
    "float" = Animation.ANIMATIONTYPE_FLOAT,
    "vector" = Animation.ANIMATIONTYPE_VECTOR3
}
export enum LOOP_MODE {
    "cycle" = Animation.ANIMATIONLOOPMODE_CYCLE
}